Run all quality checks for this project in sequence and report results:

1. `npm test` — unit tests

Run the command with the Bash tool. Print a summary showing pass or fail. If the check failed, include the relevant error output so the user knows what needs fixing.

Report only, do not attempt to fix failures.
